slight_asset: &slight_asset
  # maximum_embedded_file_size: the limit file size to decide whether or not to embed
  maximum_embedded_file_size: "15kB"

development:
  slight_asset:
    <<: *slight_asset

test:
  slight_asset:
    <<: *slight_asset

production:
  slight_asset:
    <<: *slight_asset
