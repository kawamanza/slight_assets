source :rubygems

gem "yui-compressor", :require => "yui/compressor"
gem "closure-compiler"
gem "utf8" if RUBY_VERSION.to_f < 1.9

gem "step-up", "0.6.0"

group :test do
  gem "rspec"
  gem "mocha"
end
